# Default Avatar

Place your default avatar image here as `penguin-avatar.png`.

If this file doesn't exist, the app will show the first letter of user's name as fallback.

Recommended image size: 200x200px or larger
Format: PNG with transparency
